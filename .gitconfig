[user]
	email = vickyfarenza@gmail.com
	name = Fikry C Farenza
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge --skip -- %f
	process = git-lfs filter-process --skip
	required = true
[core]
	editor = vim
